/*
const postChefReview = async (_parent, {chefID}, {models}) => {
  const errorMsg = "Could not find reviews for this chef.";

  try{
  
  } catch(error) {
    console.log(`${errorMsg} ${error}`);
    throw new Error(error);
  }
};
*/

const postChefReview = () => {}

module.exports = {
  postChefReview,
};
