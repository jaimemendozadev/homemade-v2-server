type User {
  _id: ID!
  firstName: String!
  lastName: String!
  bio: String
  status: String
  phoneNumber: String!
  profileUrl: String
  signatureURL: String
  email: String!




  likes: [Int]
  
  # Can't specify an empty array in GraphQL type
  # reviews will be an array of strings for now
  chefReviews: [Review]
  isChef: Boolean
  chefDishes: [Dish]
  location: Location
  address: Address!
  rating: Int
  
}



input UserPayload {
  firstName: String!
  lastName: String!
  bio: String
  status: String
  phoneNumber: String!
  profileUrl: String
  signatureURL: String
  email: String!



  likes: [Int]
  isChef: Boolean
  location: LocationInput
  address: AddressInput
  rating: Int
}

type Location {
  geo_lat: Float,
  geo_lng: Float,
}

input LocationInput {
  geo_lat: Float,
  geo_lng: Float,
}

type Address {
  streetNumber: String,
  city: String,
  state: String,
  postalCode: String
}

input AddressInput {
  streetNumber: String,
  city: String,
  state: String,
  postalCode: String
}

type Query {
  getUser(userID: String!): User!
  getChefReviews(chefID: String!): [Review]!
  findChefsInRange(geoCoords: LocationInput): [User]!
  findChefsByStyle(geoCoords: LocationInput!, cuisineType: String!): [User]!
}

type Mutation {
  createUser(input: UserPayload!): User!
  updateUser(authID: String!): [User]!
  addSignature(authID: String!): String!
  postUserReview(authID: String!): [String]!
  postChefReview(authID: String!): [String]!
}

# ToDo: Possibly Implement Twilio
# Twilio: textChef
